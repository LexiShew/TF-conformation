# Deprecated — stale dataset/PDB exports

Early (2026-04) exports of the JASPAR-derived dataset that predate the current
pipeline; the canonical dataset is `stage4_preprocess/jaspar_h11mo_cluster_wise_dna_containing_dataset.npy`.
Kept for provenance, not used by any code.
